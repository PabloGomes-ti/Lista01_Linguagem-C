/*24 Construa um programa em C para calcular a média de valores PARES e ÍMPARES, que serão digitados pelo
usuário. Ao final o programa deve mostrar estas duas médias. Pergunte ao usuário se ele deseja continuar a
cada iteração*/
#include<stdio.h>
#include<stdlib.h>
#include<locale.h>
#include<math.h>
int main(){
	int i;
int num,numinpar,numpar,maiornumpar,maiornuminpar;
main();
{
 while(7=7){
 }
 printf("insira um valor:");
 scanf("%i",&num);
 if(num<0){
break;
 }
 if(num%2==0){
     numpar+=num;
     if(num>maiornumpar){
         maiornumpar=num;
     }
 }else if (num%2==1){
     numinpar+=num;
     if(num>maiornuminpar){
         maiornuminpar=num;
     }
 }
}
printf("/n/n/nmedia dos valores impares :%d/nmedia dos valores pares: %d/nmaior numero impar: %d/nmaior numero par:%d",numinpar,numpar,maiornuminpar,maiornumpar)
scanf(numinpar,numpar,maiornuminpar,maiornumpar);
}

	
